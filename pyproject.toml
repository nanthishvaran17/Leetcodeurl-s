[project]
name = "college-leetcode-tracker"
version = "0.1.0"
description = "College LeetCode Performance Tracker"
readme = "README.md"
requires-python = ">=3.10"

[tool.vercel]
entrypoint = "backend.main:app"
