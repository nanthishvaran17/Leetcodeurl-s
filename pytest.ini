[pytest]
markers = 
	scale: marks tests that require a production-sized database (>=1395 records) to pass
	integration: marks tests that require live external environment connections
addopts = -m "not scale and not integration"
testpaths = tests

